services:
  pgweb:
    image: sosedoff/pgweb
    container_name: pgweb
    restart: unless-stopped
    ports:
      - "5050:8081"
    env_file: .env
    networks:
      - proxy
    labels:
      - "dev.dozzle.url=http://192.168.0.100:5050"

networks:
  proxy:
    external: true
