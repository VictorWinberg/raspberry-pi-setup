name: Raspberry Pi Healthchecks
on:
  push:
    branches:
      - add-cron-jobs
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping-endpoint:
    name: Ping Endpoint
    runs-on: ubuntu-latest
    steps:
      - run: curl -Is https://www.zolly.ml
      - run: curl -Is http://www.zolly.ml
      - run: curl -Is https://asdasd.zolly.ml
